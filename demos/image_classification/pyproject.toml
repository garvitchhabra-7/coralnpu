[project]
name = "coralnpu-image-classification-demo"
version = "0.1.0"
requires-python = ">=3.10,<3.12"
dependencies = [
    "tflite-runtime",
    "numpy<2",
    "Pillow",
    "tensorflow>=2.21.0",
]

[tool.uv]
